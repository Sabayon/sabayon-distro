diff -Nurp grub-1.99.orig/util/grub.d/10_linux.in grub-1.99/util/grub.d/10_linux.in
--- grub-1.99.orig/util/grub.d/10_linux.in	2011-06-11 01:31:15.522650425 +0200
+++ grub-1.99/util/grub.d/10_linux.in	2011-06-11 01:32:25.312650382 +0200
@@ -223,7 +223,7 @@ while [ "x$list" != "x" ] ; do
       "${GRUB_CMDLINE_LINUX} ${GRUB_CMDLINE_EXTRA} ${GRUB_CMDLINE_LINUX_DEFAULT}"
   if [ "x${GRUB_DISABLE_RECOVERY}" != "xtrue" ]; then
     linux_entry "${OS}" "${version}" true \
-	"single nomodeset ${GRUB_CMDLINE_LINUX}"
+	"single init_opts=single nomodeset ${GRUB_CMDLINE_LINUX}"
   fi
 
   list=`echo $list | tr ' ' '\n' | grep -vx $linux | tr '\n' ' '`
